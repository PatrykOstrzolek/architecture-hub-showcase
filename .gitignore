.idea
ansible/group_vars/all/vault.yml
docs/operations/mikrus-server.md
# Local dev tooling (Playwright MCP browser driver, not a project artifact)
tools/
/.playwright-mcp/
/.maister

# Terraform (secondary EC2 backend) — local state, never committed
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/terraform.tfvars
.vercel
.env*
