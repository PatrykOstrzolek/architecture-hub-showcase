---
# Intentionally empty — nginx reload is a plain task in tasks/main.yml, not a handler.
# See the comment there: changed_when: false on the preceding validate task meant a
# notify from it would never fire, so it's an unconditional task instead.
