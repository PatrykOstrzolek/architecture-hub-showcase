services:
  backend:
    image: ghcr.io/${GHCR_OWNER}/architecture-hub-backend:${IMAGE_TAG:?IMAGE_TAG must be set}
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8080"
    environment:
      APP_ENV: prod
      APP_SECRET: ${APP_SECRET}
      # ${DB_HOST} resolves to the primary's Tailscale IP here — there is no
      # local `db` service on this host. See docs/operations/infrastructure.md
      # ("EC2 secondary") and docs/operations/failover-runbook.md.
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/${DB_DATABASE}?serverVersion=16&charset=utf8
      DEFAULT_URI: ${DEFAULT_URI}
      LOCK_DSN: flock
      SEAL_DSN: loupe:///var/www/html/var/indexes
      NEXT_REVALIDATE_URL: ${NEXT_REVALIDATE_URL}
      REVALIDATE_SECRET: ${REVALIDATE_SECRET}
      PREVIEW_SECRET: ${PREVIEW_SECRET}
      PHP_MEMORY_LIMIT: 1G
    mem_limit: 768m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8080/admin/"]
      interval: 30s
      timeout: 5s
      retries: 3
    volumes:
      - uploads:/var/www/html/public/uploads

volumes:
  uploads:
