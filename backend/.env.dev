# Host-side override: the app runs on the host (via mise-managed PHP), while
# Postgres runs in Docker with its port published to the host — see
# backend/compose.override.yaml and docs/architecture/adrs/0017-mise-managed-host-php-for-local-dev.md.
DATABASE_URL="postgresql://user:ChangeMe@127.0.0.1:5432/su_myapp?serverVersion=16&charset=utf8"
